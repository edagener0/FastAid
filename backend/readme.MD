# Backend

- Aqui fica o código todo do backend

- cloudflared tunnel --url http://localhost:8000 para meter o cloudflare tunnel a rodar.
- A seguir meter o url obtido como webhook.
- nao esquecer de meter a app a executar.